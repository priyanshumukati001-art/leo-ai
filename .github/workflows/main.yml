name: Build Leo APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # अपडेटेड v4

      - name: Build with Buildozer
        uses: ArtemSerebrennikov/buildozer-action@v1
        with:
          buildozer_version: master
          command: buildozer android debug
          repository_display_name: Leo AI
          package_name: leoai
          package_domain: org.leo

      - name: Upload APK
        uses: actions/upload-artifact@v4  # यहाँ v3 की जगह v4 कर दिया है
        with:
          name: Leo-AI-Package
          path: bin/*.apk
